# E9-B02 待实云验收记录

日期：2026-09-17。状态：`active`，未通过真实云验收。

## 基线与改动

- 接手基线：`origin/main` `db19c9f`；B 侧提交：`981ce23`、`3415fed`，均已由主对话快进合入 `main`。
- 修改 `cloudfunctions/garden-api/domain.cjs`：`admin.me` 只返回角色，不再返回完整认证 UID。`admin/App.vue` 只使用角色。
- 修改 `scripts/check-community.mjs`：本地验证成员在请求体伪造 UID/角色时仍被拒绝，并断言管理员响应仅有角色。
- 修改 `cloudfunctions/garden-api/domain.cjs` 与 `repository.cjs`：将 Node 16.13 不支持的全局 `structuredClone` 替换为 `node:v8` 的序列化拷贝。B 侧用实际 Node 16.13 执行领域入口和单文档仓库读取检查。
- 未重复导入 seed，未删除或覆盖云数据，未开始 E9-B03。云函数已从 `main @3415fed` 的构建产物重新部署。

## 检查与证据边界

B 侧已通过 `corepack pnpm install --offline`、`corepack pnpm peers check`、`check:mock`、`check:assessment`、`check:experience`、`check:completeness`、`check:community`、`check:cloud-repository`、`build:mp-weixin:cloud` 和 `git diff --check`。主对话合入后复跑 `check:community`、`check:cloud-repository`、`build:mp-weixin:cloud` 和 `git diff --check`，均通过。社区检查使用本地内存服务，云仓库检查使用模拟 SDK；这些结果不证明云端权限或身份有效。

交接记录称 `garden-api` 已 Active，`garden_state`、首次 seed 和数据库/存储客户端拒绝规则已配置。本次未能独立复核：微信开发者工具 CLI `islogin` 返回已登录，但只读的云函数列表请求返回 `code 10`“需要重新登录”。CLI 无函数调用命令，本机也无可用的负责人后台登录会话。

负责人重新登录后再次复核：旧项目窗口仍占用 CLI 服务端口，其云函数与环境列表请求均返回 `ret 42001`“access_token expired”；新登录窗口未开启 CLI 服务端口。因此 `islogin=true` 不能证明云管理会话可用。已准备本机云模式后台入口 `http://127.0.0.1:5174/`，仅供负责人亲自登录后的受控验证；尚未输入账号信息，且该本机入口不等于正式 HTTPS 后台部署。

后续 CLI 连接恢复：只读环境列表包含既有环境，函数列表包含 `garden-api`。B 侧先部署 UID 脱敏修复，再确认线上 Nodejs16.13 运行时与 `structuredClone` 不兼容；主对话合入 `3415fed` 后仅二次部署 `garden-api`。部署反馈成功，函数状态 Active；从云端下载的 6 个文件与 `main @3415fed` 本地构建产物逐一 SHA256 一致。此证据证明部署版本一致，不证明函数实际调用成功。

B 侧还从全新未登录的浏览器上下文尝试云模式后台的只读 `courses.list`，SDK 在发出云请求前返回 `credentials not found`，因此不计为真实云调用。随后已用开发者工具 CLI 打开 `dist/build/mp-weixin` 项目，通过官方自动化连接 AppService 执行等效的受控只读探针；下方 Console 代码保留作人工复现方法。

2026-09-17 约 17:14–17:18 CST，B 侧进一步用官方开发者工具自动化连接当前小程序 AppService：`cli auto --project dist/build/mp-weixin --auto-port 9420` 后，`miniprogram-automator@0.12.1` 通过本地 WebSocket 连接并在 AppService 内执行 `wx.cloud.callFunction`。自动化包仅放在被 Git 忽略的本地目录，未修改依赖清单或锁文件。调用只输出动作、布尔值、数量和错误码；未读取或记录完整身份值。此段为真实云函数 RPC，未使用本地 Mock。

- `courses.list`：`ok=true`、课程数 5、默认课程在列表中；返回课程 ID 集合与首次 seed 使用的 `mockCourses` 完全一致。
- `courses.get(course-succulent)`：`ok=true`、课程 ID 匹配、步骤数 5、存在课程版本 ID。
- 同一微信模拟器身份在请求体伪造管理员 UID/角色后，`admin.me` 和 `admin.list` 均为 `ok=false`、`FORBIDDEN`；这尚不能替代真正管理员登录验证。
- 客户端直接读取 `garden_state/manifest`：拒绝，错误码 `-502003`，未返回数据。对随机专用测试文档直接写入：拒绝，`-502003`，未创建文档。
- 客户端向随机专用测试路径直接上传极小文件：拒绝，`-503002`，云端无新文件；本地临时文件已清理。
- 云存储直接读取尚无已确认可安全测试的既有文件 ID，未以不存在文件的错误冒充权限拒绝。云函数日志尚未在控制台独立核对；CLI 无日志命令。

负责人随后在本机后台亲自登录，并报告课程资源、社区帖子、评论管理、成员管理、管理员、操作记录六个标签均可见。主对话从本机 Vite 服务实际响应核对其 `VITE_DATA_MODE=cloud`、`VITE_CLOUD_ENV=cloud1-d4gdq2m679f0ef9ac`。在当前代码中，后台 `signIn()` 先完成 CloudBase 用户名密码登录，再调用 `garden-api` 的 `admin.me`；只有该调用成功返回 `role=super_admin`，六个标签才可见。云函数从运行时认证上下文取得 UID、查找已启用管理员，且部署代码的 `admin.me` 只返回角色。因此这构成负责人页面观察与代码路径相互支持的管理员身份结果摘要；主对话未读取浏览器会话或捕获原始响应、UID、令牌及云函数日志。

主对话随后复用官方 AppService 自动化通道做最小云写闭环。先只读调用 `identity.get`，确认当前微信身份已有 `pending` 成员记录（未输出成员 ID/OPENID）；再以全新请求号调用一次 `identity.ensure`，返回成功且仍为同一成员。用相同请求号、不同无害附加字段重试，得到 `CONFLICT`，表明首个请求的幂等记录已由云仓库写入并在后续调用读到。未新建成员，未修改昵称、课程或用户内容；仅留下正常的幂等请求记录。

对随机不存在的云文件做客户端读取探针得到 `-403003`，获取临时 URL 仅返回无 URL 的状态。两项都不能区分权限拒绝与文件不存在/ID 格式问题，因此仍不计为存储直读拒绝证据；未读取真实用户文件。

| 验收项 | 本次结果 |
| --- | --- |
| `garden-api` 实际调用及云端读写闭环 | 实云读取成功；既有成员的 `identity.ensure` 幂等写入闭环通过 |
| 可信管理员身份、请求体伪造不生效 | 非管理员伪造请求实云拒绝；负责人在云模式后台看到六个超级管理员标签，按代码路径证明管理员登录后的 `admin.me` 成功；未独立抓取响应 |
| `admin.me` 返回管理员角色且无敏感身份字段 | 页面观察支持 `role=super_admin`；部署代码仅返回角色，未独立捕获响应字段或云日志 |
| 读取首次 seed 课程及默认课程 | 实云列表和单课读取通过；5 门课程 ID 与 seed 一致 |
| 非管理员访问管理接口被拒绝 | 实云 `admin.me`、`admin.list` 均 `FORBIDDEN` |
| 客户端直接读写数据库、云存储被拒绝 | 数据库读/写和存储上传实云拒绝；存储有效文件直读未验证 |

## 继续验证

1. 使用已导入的 `dist/build/mp-weixin` 项目，确认固定环境 `cloud1-d4gdq2m679f0ef9ac`；`garden-api` 修复已部署。不得重新导入 seed。
2. 在小程序以真实微信身份调用 `courses.list`，记录动作、成功或错误码、课程数量、默认课程及与 seed 的摘要匹配情况。对一项课程调用 `courses.get`，确认来自云函数。

   在已打开的构建项目中，可从“调试器 → AppService Console”运行以下只读摘要探针；它不打印身份值。随后在云函数调用日志核对同一时刻的请求。不要通过进入“探索”页代替该探针，因为该页还会触发 `identity.ensure` 写入。

   ```js
   wx.cloud.init({ env: 'cloud1-d4gdq2m679f0ef9ac' });
   wx.cloud.callFunction({ name: 'garden-api', data: { action: 'courses.list', payload: {} } })
     .then(({ result }) => console.log({
       ok: result?.ok,
       code: result?.error?.code,
       count: result?.data?.items?.length,
       defaultPresent: result?.data?.items?.some(c => c.id === result?.data?.defaultCourseId),
     }))
     .catch(e => console.log({ cloudCallFailed: true, code: e?.code || e?.errCode || 'unknown' }));
   ```
3. 负责人亲自在后台输入并保管密码，调用 `admin.me`，只记录角色及响应字段名；确认没有完整 UID、OPENID、密码或令牌。分别附加和不附加伪造的 `payload.uid`/`payload.role`，确认权限结果一致。
4. 用独立非管理员身份调用 `admin.me`、`admin.list`，记录 `FORBIDDEN` 或实际错误码。
5. 从客户端尝试直接读取 `garden_state` 的 `manifest`、写入不存在的专用测试 ID，以及直接读取测试云文件和上传至测试路径，记录权限拒绝。若意外成功，立即停止并只清理本次测试产物，不触碰业务数据。
6. 在受控读写动作上验证云函数闭环，记录输入动作和结果摘要，不记录真实用户资料或身份值。全部实测通过后，主对话再核对证据并更新 B02 状态。

所有报告只使用“已识别/已脱敏”等摘要；不记录或索取完整 UID、OPENID、密码、验证码或令牌。
