# E9-B02 待实云验收记录

日期：2026-09-17。状态：`active`，未通过真实云验收。

## 基线与改动

- 接手基线：`origin/main` `db19c9f`；B 侧提交：`981ce23`、`3415fed`，均已由主对话快进合入 `main`。
- 修改 `cloudfunctions/garden-api/domain.cjs`：`admin.me` 只返回角色，不再返回完整认证 UID。`admin/App.vue` 只使用角色。
- 修改 `scripts/check-community.mjs`：本地验证成员在请求体伪造 UID/角色时仍被拒绝，并断言管理员响应仅有角色。
- 修改 `cloudfunctions/garden-api/domain.cjs` 与 `repository.cjs`：将 Node 16.13 不支持的全局 `structuredClone` 替换为 `node:v8` 的序列化拷贝。B 侧用实际 Node 16.13 执行领域入口和单文档仓库读取检查。
- 未重复导入 seed，未删除或覆盖云数据，未开始 E9-B03。云函数已从 `main @3415fed` 的构建产物重新部署。

## 检查与证据边界

B 侧已通过 `corepack pnpm install --offline`、`corepack pnpm peers check`、`check:mock`、`check:assessment`、`check:experience`、`check:completeness`、`check:community`、`check:cloud-repository`、`build:mp-weixin:cloud` 和 `git diff --check`。主对话合入后复跑 `check:community`、`check:cloud-repository`、`build:mp-weixin:cloud` 和 `git diff --check`，均通过。社区检查使用本地内存服务，云仓库检查使用模拟 SDK；这些结果不证明云端权限或身份有效。

交接记录称 `garden-api` 已 Active，`garden_state`、首次 seed 和数据库/存储客户端拒绝规则已配置。本次未能独立复核：微信开发者工具 CLI `islogin` 返回已登录，但只读的云函数列表请求返回 `code 10`“需要重新登录”。CLI 无函数调用命令，本机也无可用的负责人后台登录会话。

负责人重新登录后再次复核：旧项目窗口仍占用 CLI 服务端口，其云函数与环境列表请求均返回 `ret 42001`“access_token expired”；新登录窗口未开启 CLI 服务端口。因此 `islogin=true` 不能证明云管理会话可用。已准备本机云模式后台入口 `http://127.0.0.1:5174/`，仅供负责人亲自登录后的受控验证；尚未输入账号信息，且该本机入口不等于正式 HTTPS 后台部署。

后续 CLI 连接恢复：只读环境列表包含既有环境，函数列表包含 `garden-api`。B 侧先部署 UID 脱敏修复，再确认线上 Nodejs16.13 运行时与 `structuredClone` 不兼容；主对话合入 `3415fed` 后仅二次部署 `garden-api`。部署反馈成功，函数状态 Active；从云端下载的 6 个文件与 `main @3415fed` 本地构建产物逐一 SHA256 一致。此证据证明部署版本一致，不证明函数实际调用成功。

| 验收项 | 本次结果 |
| --- | --- |
| `garden-api` 实际调用及云端读写闭环 | 未验证 |
| 可信管理员身份、请求体伪造不生效 | 本地域测试通过；真实云未验证 |
| `admin.me` 返回管理员角色且无敏感身份字段 | 代码和本地域测试通过；新代码已部署，真实云未验证 |
| 读取首次 seed 课程及默认课程 | 未验证 |
| 非管理员访问管理接口被拒绝 | 本地域测试通过；真实云未验证 |
| 客户端直接读写数据库、云存储被拒绝 | 未验证 |

## 继续验证

1. 使用已导入的 `dist/build/mp-weixin` 项目，确认固定环境 `cloud1-d4gdq2m679f0ef9ac`；`garden-api` 修复已部署。不得重新导入 seed。
2. 在小程序以真实微信身份调用 `courses.list`，记录动作、成功或错误码、课程数量、默认课程及与 seed 的摘要匹配情况。对一项课程调用 `courses.get`，确认来自云函数。
3. 负责人亲自在后台输入并保管密码，调用 `admin.me`，只记录角色及响应字段名；确认没有完整 UID、OPENID、密码或令牌。分别附加和不附加伪造的 `payload.uid`/`payload.role`，确认权限结果一致。
4. 用独立非管理员身份调用 `admin.me`、`admin.list`，记录 `FORBIDDEN` 或实际错误码。
5. 从客户端尝试直接读取 `garden_state` 的 `manifest`、写入不存在的专用测试 ID，以及直接读取测试云文件和上传至测试路径，记录权限拒绝。若意外成功，立即停止并只清理本次测试产物，不触碰业务数据。
6. 在受控读写动作上验证云函数闭环，记录输入动作和结果摘要，不记录真实用户资料或身份值。全部实测通过后，主对话再核对证据并更新 B02 状态。

所有报告只使用“已识别/已脱敏”等摘要；不记录或索取完整 UID、OPENID、密码、验证码或令牌。
