# E9 工作迁移 Handoff（主对话 + B 侧）

> 迁移日期：2026-09-17。接手对象：新的主对话与新的 B 侧任务。事实以仓库最新 `origin/main` 为准；本文件不包含管理员 UID、密码、OPENID 或真实用户资料。

## 1. 项目与当前目标

项目是护理学院大创“园艺疗法小程序”。原 P01→P13 展示主链已完成；当前第九阶段只负责把已实现的多人社区、日常照片记录、点赞评论、官方内容、课程管理与社区管理部署到微信云开发并完成真实环境验收，不新增功能。

固定云环境：`cloud1-d4gdq2m679f0ef9ac`。固定小程序 AppID：`wxd5240621858fb1b8`。云函数：`garden-api`。数据库集合：`garden_state`。本地 `community-server` 仅用于早期本地联调，禁止部署为生产后端。

## 2. 接手前阅读与命令

阅读顺序：

1. `PLAN.md`
2. `STATUS.md`
3. `docs/13_第九阶段云部署与真实环境验收任务单.md`
4. 本文件
5. B 侧再读 `docs/handoffs/E9_B_云服务与权限.md`
6. A 侧再读 `docs/handoffs/E9_A_云端页面与后台.md`

接手命令：

```powershell
git switch main
git pull --ff-only origin main
git status --short --branch
```

要求工作区干净。不要从旧的 `feat/assessment-data` 远端提交继续追加；B 应先让本地协作分支快进或重建到最新 `origin/main`，再开始新的代码变更。云端验证本身若不改代码，可以只提交证据报告，不制造空提交。

## 3. 已完成且不得重复的事项

### 主对话已完成

- 本地社区、日记、点赞评论、官方内容、课程/社区后台实现已经合入 `main`。
- E9-M01 基线、任务单与 A/B handoff 已发布。
- `.env.local` 已配置云模式且被 Git 忽略；不得提交本地环境变量。
- 云模式小程序、后台和本地自动检查曾通过；这不等于真实云验收通过。

### B 侧已完成

- E9-B01 已完成。
- `garden-api` 已部署至固定环境，状态为 Active。
- 云端下载代码与本地构建产物哈希一致。
- 未部署 `community-server`。
- E9-B01 自动检查通过，未产生待推送代码。

### 项目负责人已完成的 B02 人工检查点

- 已创建 `garden_state`。
- 数据库客户端直接读写已设为拒绝。
- 云存储客户端直接读写已设为拒绝。
- CloudBase 用户名密码登录已开启。
- 管理员账号已由负责人创建并保管密码。
- 管理员 seed 已在本机生成。
- seed 已使用 Insert 模式首次导入；集合中可见 `manifest` 与管理员记录。

禁止再次生成并导入同一 seed，避免 `_id` 冲突或覆盖云端数据。禁止要求负责人把 UID、密码、验证码、令牌或完整 OPENID 发到对话中。

## 4. 当前唯一进行中的关键任务：E9-B02

状态：`active`，尚未完成。

B 侧接手后只做真实云验证和必要的最小修复：

1. 验证小程序或受控客户端实际调用 `garden-api`，确认不是本地 Mock 成功。
2. 验证云函数从可信上下文识别当前管理员，不能由请求体中的 UID/角色伪造。
3. 验证 `admin.me` 能返回管理员身份；响应不得包含密码、令牌或完整敏感身份信息。
4. 验证课程读取能返回 seed 中的初始课程。
5. 用非管理员身份验证管理接口拒绝访问。
6. 验证客户端无法绕过云函数直接读写 `garden_state` 和云存储。
7. 若发现代码缺陷，只做完成 B02 所需的最小修复，运行 B handoff 中的全部检查后推送 `feat/assessment-data`，不得自行合并 `main`。

B02 交付报告必须包含：任务 ID、基线/提交哈希、是否改代码、云函数实际调用结果、`admin.me` 结果摘要、课程读取结果摘要、管理员/非管理员边界、客户端直读写拒绝结果、未验证项、复现步骤。报告中对 UID、OPENID 只写“已识别/已脱敏”，不写完整值。

## 5. 主对话接手职责

1. 先确认接手仓库为最新 `origin/main`，不得把旧分支误当新基线。
2. 接收 B02 报告后逐项核验证据；仅在全部通过时把 B02 标记为 `completed`。
3. 如 B 有代码提交，先 Review、运行检查、合入 `main` 并推送；如无代码变更，只更新 `PLAN.md`、`STATUS.md` 和任务单证据。
4. B02 完成后才发布 E9-B03，同时允许 A 执行 E9-A02、E9-A03；共享云环境测试时间由主对话协调。
5. A/B 完成后，主对话在同一候选提交上串行执行 E9-C01 双账号全流程和 E9-C02 备份冻结。
6. 不把人工内容审核描述成平台自动内容安全审核。

## 6. A 侧当前信息（供主对话协调）

- E9-A01 可与 B02 并行，状态仍是 `active`，没有真实云完成结论。
- `admin/App.vue` 已补充 CloudBase 非标准错误对象的可见错误提示兼容处理；该修复只改善失败可见性，不证明后台真实登录或接口已经通过。
- E9-A02、E9-A03 仍依赖 B02 完成，不得提前标记完成。

## 7. 后续顺序

```text
E9-B02 真实云身份与初始数据闭环（当前）
→ E9-B03 权限/多人数据验收
  与 E9-A02 后台真实云接入、E9-A03 真机视觉复核并行
→ E9-C01 双账号完整业务验收
→ E9-C02 备份、冻结与交付
```

E9-A01 可在 B02 期间继续，但不改变上述关键依赖。

## 8. 安全与禁止事项

- 不在聊天、提交、日志截图或文档中记录完整管理员 UID、OPENID、密码、验证码、令牌和真实用户资料。
- 不重复导入 seed，不删除或覆盖 `garden_state`，不新建第二个云环境。
- 不放宽数据库或存储为客户端可直接读写。
- 不部署 `community-server`，不增加私聊、关注、商业化或自动定时精选。
- 项目负责人需要登录时，由负责人亲自在页面输入密码；协作者不得索取密码。

## 9. 恢复后的第一条 B 侧指令

```text
从最新 origin/main 恢复 E9-B02。人工前置已完成：garden_state、数据库/存储客户端拒绝权限、用户名密码登录、管理员账号、seed 首次 Insert 导入和 manifest 可见。不要重复导入，不要索取 UID 或密码。请只补齐 garden-api 实际调用、可信管理员身份、admin.me、课程读取、非管理员拒绝和客户端直读写拒绝证据；必要时做最小修复。完成后按 handoff 格式汇报，未全部通过前不要开始 B03，也不要自行合并 main。
```
